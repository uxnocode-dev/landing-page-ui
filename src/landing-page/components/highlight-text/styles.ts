import styled from 'styled-components'
import tw from 'twin.macro'

const Part = styled.span`
    ${tw`transition-all duration-500`}
`

const Highlight = styled.span``

export default { Part, Highlight }
